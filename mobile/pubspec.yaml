name: beautyasist_mobile
description: "BeautyAsist güzellik merkezi yönetim mobil uygulaması."
# The following line prevents the package from being accidentally published to
# pub.dev using `flutter pub publish`. This is preferred for private packages.
publish_to: 'none' # Remove this line if you wish to publish to pub.dev

# The following defines the version and build number for your application.
# A version number is three numbers separated by dots, like 1.2.43
# followed by an optional build number separated by a +.
# Both the version and the builder number may be overridden in flutter
# build by specifying --build-name and --build-number, respectively.
# In Android, build-name is used as versionName while build-number used as versionCode.
# Read more about Android versioning at https://developer.android.com/studio/publish/versioning
# In iOS, build-name is used as CFBundleShortVersionString while build-number is used as CFBundleVersion.
# Read more about iOS versioning at
# https://developer.apple.com/library/archive/documentation/General/Reference/InfoPlistKeyReference/Articles/CoreFoundationKeys.html
# In Windows, build-name is used as the major, minor, and patch parts
# of the product and file versions while build-number is used as the build suffix.
# App Store'a 1.0 (5) yüklenip reddedildi (3.2.2(v) + 2.1 + 5.1.1(v)). Apple aynı build
# numarasını İKİNCİ KEZ KABUL ETMEZ; düzeltmelerin yeni yüklemesi 6'dan devam eder.
# iOS Info.plist bu değeri $(FLUTTER_BUILD_NUMBER) üzerinden okur — tek kaynak burasıdır.
version: 1.0.0+6

environment:
  sdk: '>=3.9.0 <4.0.0'

# Dependencies specify other packages that your package needs in order to work.
# To automatically upgrade your package dependencies to the latest versions
# consider running `flutter pub upgrade --major-versions`. Alternatively,
# dependencies can be manually updated by changing the version numbers below to
# the latest version available on pub.dev. To see which dependencies have newer
# versions available, run `flutter pub outdated`.
dependencies:
  flutter:
    sdk: flutter
  flutter_localizations:
    sdk: flutter

  cupertino_icons: ^1.0.8
  dio: ^5.9.2
  flutter_riverpod: ^3.3.1
  flutter_secure_storage: ^10.0.0
  go_router: ^17.1.0
  google_fonts: ^8.0.2
  intl: ^0.20.2
  image_picker: ^1.1.2
  url_launcher: ^6.3.1

  # KVKK aydınlatma metni için markalı PDF üretimi + paylaş/yazdır
  # Hediye kartındaki QR'ı çizmek için. `pdf` zaten dolaylı olarak kullanıyordu; kart görselinde
  # doğrudan kullandığımız için açık bağımlılık hâline getirildi (dolaylıya güvenmek kırılgandır).
  barcode: ^2.2.9
  pdf: ^3.11.1
  printing: ^5.13.4

  # Canlı bildirim altyapısı:
  #  - flutter_local_notifications + timezone: yerel + zamanlanmış bildirim (internetsiz/kapalıyken çalışır)
  #  - flutter_background_service: uygulama arka plandayken LAN feed'i yoklayan ön plan servisi
  #  - firebase_core/messaging: OPSİYONEL uzaktan push (FCM). google-services.json yoksa guard'lı kod
  #    Firebase.initializeApp() hatasını yutar → uygulama yerel+LAN ile normal çalışır (iskele hazır).
  flutter_local_notifications: ^18.0.1
  timezone: ^0.9.4
  flutter_background_service: ^5.1.0
  firebase_core: ^3.8.0
  firebase_messaging: ^15.1.5
  mobile_scanner: ^7.4.0

  # Excel ile toplu içeri aktarma (web ImportDialog paritesi):
  #  - file_picker: cihazdan .xlsx/.xls seçimi (yerel eklenti)
  #  - excel: saf Dart çözümleyici; sayfa/satır/hücre okur, native bağımlılığı yok
  file_picker: ^11.0.3
  excel: ^4.0.6

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_launcher_icons: ^0.14.4

  # The "flutter_lints" package below contains a set of recommended lints to
  # encourage good coding practices. The lint set provided by the package is
  # activated in the `analysis_options.yaml` file located at the root of your
  # package. See that file for information about deactivating specific lint
  # rules and activating additional ones.
  flutter_lints: ^6.0.0

# For information on the generic Dart part of this file, see the
# following page: https://dart.dev/tools/pub/pubspec

# The following section is specific to Flutter packages.
flutter:

  # The following line ensures that the Material Icons font is
  # included with your application, so that you can use the icons in
  # the material Icons class.
  uses-material-design: true
  assets:
    - assets/images/
    - assets/fonts/

flutter_launcher_icons:
  android: true
  ios: true
  image_path: assets/images/logo.png

  # ARKA PLAN SAYDAM — logonun etrafındaki beyaz kutuyu kaldırır.
  #
  # Android 8+ "adaptive icon" iki katmandır: arka plan + ön plan. Buraya bir RENK
  # yazıldığında (eskiden #FFF7FA) launcher logoyu o rengin üstüne PLAKA olarak koyar;
  # kullanıcının gördüğü beyaz kutu buydu. Logonun kendisinde zaten gerçek saydamlık var
  # (%52.7 tamamen saydam piksel), bu yüzden arka planı saydam yapmak yeterli: yalnız
  # logo görünür. Değer #AARRGGBB biçimindedir; AA=00 → tamamen saydam.
  adaptive_icon_background: "#00000000"
  adaptive_icon_foreground: assets/images/logo.png

  # Plaka kalkınca logo görsel olarak küçülür (launcher maskesi + güvenli alan payı).
  # Varsayılan %16 kenar boşluğu yerine %6: logo diğer uygulama ikonlarıyla aynı ağırlıkta durur.
  adaptive_icon_foreground_inset: 6

  # iOS İKONLARI SAYDAM OLAMAZ — App Store saydam/alfa içeren ikonu reddeder.
  # Alfa kaldırılıp marka kremi ile doldurulur (Android'i etkilemez).
  remove_alpha_ios: true
  background_color_ios: "#FFF7FA"

  # To add assets to your application, add an assets section, like this:
  # assets:
  #   - images/a_dot_burr.jpeg
  #   - images/a_dot_ham.jpeg

  # An image asset can refer to one or more resolution-specific "variants", see
  # https://flutter.dev/to/resolution-aware-images

  # For details regarding adding assets from package dependencies, see
  # https://flutter.dev/to/asset-from-package

  # To add custom fonts to your application, add a fonts section here,
  # in this "flutter" section. Each entry in this list should have a
  # "family" key with the font family name, and a "fonts" key with a
  # list giving the asset and other descriptors for the font. For
  # example:
  # fonts:
  #   - family: Schyler
  #     fonts:
  #       - asset: fonts/Schyler-Regular.ttf
  #       - asset: fonts/Schyler-Italic.ttf
  #         style: italic
  #   - family: Trajan Pro
  #     fonts:
  #       - asset: fonts/TrajanPro.ttf
  #       - asset: fonts/TrajanPro_Bold.ttf
  #         weight: 700
  #
  # For details regarding fonts from package dependencies,
  # see https://flutter.dev/to/font-from-package
