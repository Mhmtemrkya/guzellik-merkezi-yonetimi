{
  "Urls": "http://localhost:5019",
  "ConnectionStrings": {
    "DefaultConnection": "Host=localhost;Port=5432;Database=guzellik_merkezi;Username=postgres;Password=change-me"
  },
  "Database": {
    "_comment": "Set SeedDemoData=true to run the demo seed on a NEW device/server or in PRODUCTION (env: Database__SeedDemoData=true). One-shot: create DB + apply EF migrations + seed demo data; idempotent (skips if a tenant already exists). SECURITY: demo accounts use the known password 'Guzellik123!' — change them immediately in production. SeedReferenceData=true seeds only default subscription plans (no demo data). RecreateOnStartup=true is DEV-ONLY: drops and rebuilds the DB.",
    "SeedDemoData": false
  },
  "Jwt": {
    "Issuer": "GuzellikMerkezi",
    "Audience": "GuzellikMerkezi.Client",
    "SigningKey": "change-this-to-a-long-random-secret"
  },
  "Encryption": {
    "_comment": "32-byte base64 key (AES-256). Use environment variables or secret store in production.",
    "MasterKeyBase64": "replace-with-32-byte-base64-key"
  },
  "WhatsApp": {
    "_comment": "Meta Cloud API. AppSecret = webhook imza (X-Hub-Signature-256) doğrulaması için (Meta App Dashboard → Settings → Basic → App Secret). ÜRETİMDE WhatsApp__AppSecret env ile verilmeli; tanımsızsa canlıda gelen webhook'lar imzasız kabul edilmez (fail-closed), Development'ta simülasyon için izin verilir. VerifyToken = webhook aboneliği handshake. PublicBaseUrl = webhook/medya URL'leri için public domain.",
    "AppSecret": "",
    "VerifyToken": "",
    "PublicBaseUrl": ""
  },
  "ForwardedHeaders": {
    "_comment": "Reverse proxy arkasında gerçek istemci IP'si (rate-limit + audit/güvenlik logları doğru IP'yi görsün). Aynı sunucudaki proxy (nginx/IIS) için ek ayar gerekmez — yalnız loopback güvenilir. Cloud LB için: TrustAll=true (LB dış X-Forwarded-For'u ezmeli/eklemeli) VEYA KnownProxies'e LB IP'lerini tek tek ekleyin. TrustAll'ı gelişigüzel açmayın (XFF spoof riski).",
    "TrustAll": false,
    "KnownProxies": [],
    "ForwardLimit": 1
  },
  "Cors": {
    "AllowedOrigins": [
      "http://localhost:3000",
      "http://localhost:5173",
      "http://127.0.0.1:3000",
      "http://127.0.0.1:5173"
    ]
  },
  "Logging": {
    "LogLevel": {
      "Default": "Information",
      "Microsoft.AspNetCore": "Warning"
    }
  },
  "AllowedHosts": "*"
}
